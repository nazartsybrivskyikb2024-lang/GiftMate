services:
  - type: web
    name: giftmate-web
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn giftmate_project.wsgi:application --workers 3 --bind 0.0.0.0:$PORT"
